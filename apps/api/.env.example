# NestJS API environment
PORT=4000
NODE_ENV=development

# Database & cache
DATABASE_URL=postgresql://forum_user:forum_pass@localhost:5432/forum_dev
REDIS_URL=redis://localhost:6379/0

# Mailhog SMTP (local dev)
MAILHOG_HOST=localhost
MAILHOG_PORT=1025
MAIL_FROM_ADDRESS=forum@example.com

# App URLs
PUBLIC_APP_URL=http://localhost:3000

# JWT secrets (use stronger secrets in production)
JWT_ACCESS_TOKEN_SECRET=dev_access_token_secret_change_me_please_
JWT_ACCESS_TOKEN_TTL=15m
JWT_REFRESH_TOKEN_SECRET=dev_refresh_token_secret_change_me_now
JWT_REFRESH_TOKEN_TTL=30d
